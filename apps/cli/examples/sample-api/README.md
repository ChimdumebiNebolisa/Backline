This sample was created by `backline sample init`. Build the sample API JAR (`./gradlew :apps:sample-api:bootJar`), run `backline sample serve` in another terminal, ensure your Backline API is reachable (`BACKLINE_API_URL`), then run `backline run` from this directory or with `-f` pointing at `backline.yml`. For full setup, testing, and the demo path, see the repository root `README.md` and the `docs/` folder.
